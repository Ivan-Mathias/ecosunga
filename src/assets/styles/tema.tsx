import { createMuiTheme } from "@material-ui/core";

export const Tema = createMuiTheme({
    palette: {
        primary: {
            main: '#02C532'
        }
    },
    typography: {
        fontFamily: "Archivo",
    }
});