import { createMuiTheme } from "@material-ui/core";

export const Tema = createMuiTheme({
    palette: {
        primary: {
            main: '#02C532'
        },
        secondary: {
            main: '#132C84'
        }
    },
    typography: {
        fontFamily: "Archivo",
    }
});